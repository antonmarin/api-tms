version: "3.7"
services:
  php:
    build:
      context: backend
      dockerfile: build/Dockerfile
      target: php_runtime
    volumes:
      - "./backend:/app"
  nginx:
    build:
      context: frontend
      dockerfile: build/Dockerfile
      target: nginx_runtime
    depends_on:
      - php
    volumes:
      - "./frontend:/app"
    ports:
      - "8080:80"
